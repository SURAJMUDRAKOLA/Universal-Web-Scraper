{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "setup-venv",
			"type": "shell",
			"command": "cmd",
			"args": [
				"/c",
				"python -m venv venv && call venv\\Scripts\\activate && pip install --upgrade pip && pip install -r requirements.txt && playwright install"
			],
			"problemMatcher": [],
			"presentation": {
				"reveal": "silent"
			}
		}
	]
}